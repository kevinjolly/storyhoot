$('#search-results-users').append('<%= escape_javascript( render partial: 'shared/user', collection: @users ) %>');
<% if @users.next_page %>
  $('.pagination').replaceWith('<%= escape_javascript( will_paginate(@users) ) %>');
<% else %>
  $('.pagination').text('No more users to show');
<% end %>
<% sleep 1 %>