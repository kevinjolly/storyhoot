<div class="row-collapse" id="discover-page">
  <% @books.each do |book| %>
    <div class="large-1 medium-2 small-3 columns end">
      <%= link_to image_tag(book.cover.url(:thumb)), book %>
    </div>
  <% end %>
</div>